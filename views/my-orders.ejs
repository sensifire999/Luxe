<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">
    <title>My Orders | LuxeStore History</title>
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; background-color: #F8FAFC; -webkit-tap-highlight-color: transparent; }
        .order-card { transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); }
        .order-card:active { transform: scale(0.96); }
        .glass-nav { background: rgba(255, 255, 255, 0.85); backdrop-filter: blur(20px); }
    </style>
</head>
<body class="pb-32">

    <div class="max-w-md mx-auto px-6 pt-10">
        <div class="flex items-center justify-between mb-10">
            <div>
                <h1 class="text-4xl font-black italic uppercase tracking-tighter leading-none">My <span class="text-blue-600">Orders</span></h1>
                <p class="text-[10px] font-bold text-slate-400 uppercase tracking-[3px] mt-2">Purchase History & Tracking</p>
            </div>
            <div class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center shadow-sm border border-slate-100">
                <svg class="w-6 h-6 text-slate-900" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2"><path d="M16 11V7a4 4 0 11-8 0v4M5 9h14l1 12H4L5 9z"/></svg>
            </div>
        </div>

        <div class="space-y-6">
            <% if(orders && orders.length > 0) { %>
                <% orders.forEach(order => { %>
                    <a href="/order-track/<%= order.id %>" class="block order-card bg-white p-5 rounded-[2.5rem] border border-white shadow-xl shadow-slate-200/50 relative overflow-hidden group">
                        
                        <div class="flex justify-between items-start mb-4">
                            <span class="text-[9px] font-black text-slate-400 uppercase tracking-widest bg-slate-50 px-3 py-1 rounded-full border border-slate-100">ID: #<%= order.id %></span>
                            <span class="text-[9px] font-black text-slate-400 uppercase tracking-tighter opacity-60"><%= order.date %></span>
                        </div>

                        <div class="flex gap-5 items-center">
                            <div class="w-24 h-24 bg-slate-50 rounded-[1.8rem] overflow-hidden shrink-0 border border-slate-100 shadow-inner">
                                <% if (order.items && order.items.length > 0) { %>
                                    <% let item = order.items[0]; %>
                                    <% if (Array.isArray(item.images) && item.images.length > 0) { %>
                                        <img src="<%= item.images[0] %>" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500">
                                    <% } else { %>
                                        <img src="<%= item.image %>" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500">
                                    <% } %>
                                <% } %>
                            </div>

                            <div class="flex-grow">
                                <h3 class="font-black text-sm uppercase italic leading-tight mb-1 truncate w-32"><%= order.items[0].name %></h3>
                                <div class="flex items-center gap-1 mb-3">
                                    <p class="text-blue-600 font-black italic text-xl">₹<%= order.total %></p>
                                    <span class="text-[8px] text-slate-400 font-bold uppercase ml-1 italic">Total Paid</span>
                                </div>
                                
                                <% 
                                    let statusStyle = "bg-amber-50 text-amber-600 border-amber-100";
                                    if(order.status.includes("Shipped")) statusStyle = "bg-blue-50 text-blue-600 border-blue-100";
                                    if(order.status.includes("Delivered")) statusStyle = "bg-emerald-50 text-emerald-600 border-emerald-100";
                                    if(order.status.includes("Cancel")) statusStyle = "bg-red-50 text-red-600 border-red-100";
                                %>
                                <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-xl border <%= statusStyle %>">
                                    <div class="w-1.5 h-1.5 rounded-full bg-current animate-pulse"></div>
                                    <span class="text-[9px] font-black uppercase tracking-wider"><%= order.status %></span>
                                </div>
                            </div>

                            <div class="w-8 h-8 rounded-full bg-slate-50 flex items-center justify-center text-slate-300 group-hover:bg-blue-600 group-hover:text-white transition-all">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="3"><path d="M9 5l7 7-7 7"/></svg>
                            </div>
                        </div>
                    </a>
                <% }) %>

                <div class="mt-12 bg-slate-900 rounded-[2.8rem] p-8 text-white relative overflow-hidden shadow-2xl shadow-blue-900/30">
                    <div class="absolute -right-6 -top-6 w-32 h-32 bg-green-500 rounded-full blur-[60px] opacity-20"></div>
                    
                    <div class="relative z-10 text-center">
                        <div class="inline-block bg-green-500/10 text-green-400 px-4 py-1 rounded-full text-[9px] font-black uppercase tracking-widest mb-4">Community Support</div>
                        <h3 class="text-2xl font-black italic uppercase tracking-tighter mb-6">VIP <span class="text-green-500">Luxe</span> Group</h3>
                        
                        <a href="https://chat.whatsapp.com/GfbhmhHpZunC5IN6e96UaM" 
                           target="_blank"
                           class="block w-full bg-[#25D366] hover:bg-[#128C7E] py-5 rounded-[1.8rem] font-black uppercase text-[11px] tracking-[0.2em] shadow-xl shadow-green-600/20 transition-all active:scale-95">
                            Join Support Group
                        </a>
                        
                        <p class="text-[9px] font-bold text-slate-500 uppercase mt-5 tracking-widest">
                            Exclusive Tracking Updates & Support
                        </p>
                    </div>
                </div>

            <% } else { %>
                <div class="text-center py-24">
                    <div class="w-32 h-32 bg-white rounded-[2.5rem] flex items-center justify-center mx-auto mb-8 shadow-2xl shadow-slate-200 rotate-6 border border-slate-50">
                        <span class="text-5xl opacity-40">🛍️</span>
                    </div>
                    <h2 class="text-2xl font-black italic uppercase tracking-tighter">No History</h2>
                    <p class="text-slate-400 text-[10px] font-bold uppercase tracking-[4px] mt-4 mb-10 leading-relaxed">Start your journey with <br>premium selections.</p>
                    <a href="/" class="inline-block bg-slate-900 text-white px-10 py-5 rounded-2xl font-black uppercase text-[10px] tracking-[0.2em] shadow-xl active:scale-95 transition-all">Start Shopping</a>
                </div>
            <% } %>
        </div>
    </div>

    <div class="fixed bottom-6 left-1/2 -translate-x-1/2 w-[92%] glass-nav border border-white/50 px-8 py-5 flex justify-between items-center z-50 rounded-[2.5rem] shadow-2xl">
        <a href="/" class="text-slate-300"><svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/></svg></a>
        <a href="/my-orders" class="text-blue-600 relative">
            <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M19 20H5V10h14v10zM5 8V4h14v4H5z"/></svg>
            <span class="absolute -top-1 -right-1 w-2 h-2 bg-red-500 rounded-full border border-white"></span>
        </a>
        <a href="/cart" class="text-slate-300"><svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"/></svg></a>
        <a href="/profile" class="text-slate-300"><svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path d="M16 7a4 4 0 11-8 0v4M12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg></a>
    </div>

</body>
</html>

